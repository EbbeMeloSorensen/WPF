<UserControl x:Class="KeyboardInput3.Views.SubView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KeyboardInput3.Views"
             xmlns:behaviors="clr-namespace:KeyboardInput3.Behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="This TextBox will get focus automatically:" Margin="0 0 0 5" />

        <TextBox Width="200"
                 behaviors:FocusBehavior.RequestFocus="{Binding ShouldFocusInput}"
                 Text="{Binding InputText}" />
    </StackPanel>
</UserControl>
